<Window x:Class="DevSticky.Views.KanbanWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:DevSticky.Views"
        xmlns:loc="clr-namespace:DevSticky.Services"
        Title="{loc:Localize KanbanTitle}"
        Width="900" Height="600"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResize">
    
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/SharedStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            
            <!-- Kanban Card Style -->
            <Style x:Key="KanbanCardStyle" TargetType="Border">
                <Setter Property="Background" Value="{DynamicResource Surface1Brush}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource Surface2Brush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="6"/>
                <Setter Property="Margin" Value="4"/>
                <Setter Property="Padding" Value="8"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{DynamicResource Surface2Brush}"/>
                        <Setter Property="BorderBrush" Value="{DynamicResource BlueBrush}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
            
            <!-- Column Header Style -->
            <Style x:Key="ColumnHeaderStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="16"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                <Setter Property="HorizontalAlignment" Value="Center"/>
                <Setter Property="Margin" Value="0,8"/>
            </Style>
        </ResourceDictionary>
    </Window.Resources>
    
    <Border Background="{DynamicResource BaseBrush}" CornerRadius="8" 
            BorderBrush="{DynamicResource Surface1Brush}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- Header -->
            <Border Grid.Row="0" Background="{DynamicResource Surface0Brush}" 
                    CornerRadius="8,8,0,0" MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <Grid Margin="12,0">
                    <TextBlock Text="{loc:Localize KanbanBoard}" 
                               Foreground="{DynamicResource TextBrush}" 
                               FontSize="14" FontWeight="SemiBold"
                               VerticalAlignment="Center"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <Button Content="🔄" ToolTip="{loc:Localize Refresh}" 
                                Style="{StaticResource IconBtn}" 
                                Click="BtnRefresh_Click" Margin="0,0,8,0"/>
                        <Button Content="✕" ToolTip="{loc:Localize Close}" 
                                Style="{StaticResource IconBtn}" 
                                Click="BtnClose_Click"/>
                    </StackPanel>
                </Grid>
            </Border>
            
            <!-- Kanban Board -->
            <Grid Grid.Row="1" Margin="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- To Do Column -->
                <Border Grid.Column="0" Background="{DynamicResource Surface0Brush}" 
                        CornerRadius="6" Margin="4" BorderBrush="{DynamicResource Surface2Brush}" BorderThickness="1">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <TextBlock Grid.Row="0" Text="{loc:Localize ToDo}" Style="{StaticResource ColumnHeaderStyle}"/>
                        
                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                            <ItemsControl x:Name="ToDoItems" Margin="8">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Style="{StaticResource KanbanCardStyle}" 
                                                MouseLeftButtonDown="Card_MouseLeftButtonDown"
                                                AllowDrop="True"
                                                Drop="Card_Drop"
                                                DragEnter="Card_DragEnter"
                                                DragLeave="Card_DragLeave">
                                            <Grid>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                </Grid.RowDefinitions>
                                                
                                                <TextBlock Grid.Row="0" Text="{Binding Title}" 
                                                           FontWeight="SemiBold" FontSize="12"
                                                           Foreground="{DynamicResource TextBrush}"
                                                           TextWrapping="Wrap" MaxHeight="40"/>
                                                
                                                <TextBlock Grid.Row="1" Text="{Binding ContentPreview}" 
                                                           FontSize="10" Margin="0,4,0,0"
                                                           Foreground="{DynamicResource Text2Brush}"
                                                           TextWrapping="Wrap" MaxHeight="60"/>
                                                
                                                <ItemsControl Grid.Row="2" ItemsSource="{Binding Tags}" Margin="0,4,0,0">
                                                    <ItemsControl.ItemsPanel>
                                                        <ItemsPanelTemplate>
                                                            <WrapPanel/>
                                                        </ItemsPanelTemplate>
                                                    </ItemsControl.ItemsPanel>
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Border Background="{DynamicResource BlueBrush}" 
                                                                    CornerRadius="2" Padding="4,2" Margin="2">
                                                                <TextBlock Text="{Binding}" FontSize="8" 
                                                                           Foreground="{DynamicResource BaseBrush}"/>
                                                            </Border>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </Border>
                
                <!-- In Progress Column -->
                <Border Grid.Column="1" Background="{DynamicResource Surface0Brush}" 
                        CornerRadius="6" Margin="4" BorderBrush="{DynamicResource Surface2Brush}" BorderThickness="1">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <TextBlock Grid.Row="0" Text="{loc:Localize InProgress}" Style="{StaticResource ColumnHeaderStyle}"/>
                        
                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                            <ItemsControl x:Name="InProgressItems" Margin="8">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Style="{StaticResource KanbanCardStyle}" 
                                                MouseLeftButtonDown="Card_MouseLeftButtonDown"
                                                AllowDrop="True"
                                                Drop="Card_Drop"
                                                DragEnter="Card_DragEnter"
                                                DragLeave="Card_DragLeave">
                                            <Grid>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                </Grid.RowDefinitions>
                                                
                                                <TextBlock Grid.Row="0" Text="{Binding Title}" 
                                                           FontWeight="SemiBold" FontSize="12"
                                                           Foreground="{DynamicResource TextBrush}"
                                                           TextWrapping="Wrap" MaxHeight="40"/>
                                                
                                                <TextBlock Grid.Row="1" Text="{Binding ContentPreview}" 
                                                           FontSize="10" Margin="0,4,0,0"
                                                           Foreground="{DynamicResource Text2Brush}"
                                                           TextWrapping="Wrap" MaxHeight="60"/>
                                                
                                                <ItemsControl Grid.Row="2" ItemsSource="{Binding Tags}" Margin="0,4,0,0">
                                                    <ItemsControl.ItemsPanel>
                                                        <ItemsPanelTemplate>
                                                            <WrapPanel/>
                                                        </ItemsPanelTemplate>
                                                    </ItemsControl.ItemsPanel>
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Border Background="{DynamicResource BlueBrush}" 
                                                                    CornerRadius="2" Padding="4,2" Margin="2">
                                                                <TextBlock Text="{Binding}" FontSize="8" 
                                                                           Foreground="{DynamicResource BaseBrush}"/>
                                                            </Border>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </Border>
                
                <!-- Done Column -->
                <Border Grid.Column="2" Background="{DynamicResource Surface0Brush}" 
                        CornerRadius="6" Margin="4" BorderBrush="{DynamicResource Surface2Brush}" BorderThickness="1">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <TextBlock Grid.Row="0" Text="{loc:Localize Done}" Style="{StaticResource ColumnHeaderStyle}"/>
                        
                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                            <ItemsControl x:Name="DoneItems" Margin="8">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Style="{StaticResource KanbanCardStyle}" 
                                                MouseLeftButtonDown="Card_MouseLeftButtonDown"
                                                AllowDrop="True"
                                                Drop="Card_Drop"
                                                DragEnter="Card_DragEnter"
                                                DragLeave="Card_DragLeave">
                                            <Grid>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                </Grid.RowDefinitions>
                                                
                                                <TextBlock Grid.Row="0" Text="{Binding Title}" 
                                                           FontWeight="SemiBold" FontSize="12"
                                                           Foreground="{DynamicResource TextBrush}"
                                                           TextWrapping="Wrap" MaxHeight="40"/>
                                                
                                                <TextBlock Grid.Row="1" Text="{Binding ContentPreview}" 
                                                           FontSize="10" Margin="0,4,0,0"
                                                           Foreground="{DynamicResource Text2Brush}"
                                                           TextWrapping="Wrap" MaxHeight="60"/>
                                                
                                                <ItemsControl Grid.Row="2" ItemsSource="{Binding Tags}" Margin="0,4,0,0">
                                                    <ItemsControl.ItemsPanel>
                                                        <ItemsPanelTemplate>
                                                            <WrapPanel/>
                                                        </ItemsPanelTemplate>
                                                    </ItemsControl.ItemsPanel>
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Border Background="{DynamicResource BlueBrush}" 
                                                                    CornerRadius="2" Padding="4,2" Margin="2">
                                                                <TextBlock Text="{Binding}" FontSize="8" 
                                                                           Foreground="{DynamicResource BaseBrush}"/>
                                                            </Border>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>