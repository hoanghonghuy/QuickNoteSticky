<Window x:Class="DevSticky.Views.TagManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:DevSticky.Services"
        Title="{loc:Localize ManageTagsTitle}"
        Width="400" Height="450"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize">
    
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/SharedStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            
            <Style x:Key="ColorBtn" TargetType="Button">
            <Setter Property="Width" Value="24"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" 
                                CornerRadius="4" BorderThickness="2" BorderBrush="Transparent">
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="BorderBrush" Value="{DynamicResource TextBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        </ResourceDictionary>
    </Window.Resources>
    
    <Border Background="{DynamicResource BaseBrush}" CornerRadius="8" 
            BorderBrush="{DynamicResource Surface1Brush}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="50"/>
            </Grid.RowDefinitions>
            
            <!-- Header -->
            <Border Grid.Row="0" Background="{DynamicResource Surface0Brush}" 
                    CornerRadius="8,8,0,0" MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <Grid Margin="12,0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="🏷️" FontSize="14" Margin="0,0,8,0"/>
                        <TextBlock Text="{loc:Localize ManageTagsTitle}" Foreground="{DynamicResource TextBrush}" 
                                   FontSize="14" FontWeight="SemiBold"/>
                    </StackPanel>
                    <Button Content="✕" Style="{StaticResource IconBtn}" 
                            HorizontalAlignment="Right" Click="BtnClose_Click"/>
                </Grid>
            </Border>
            
            <!-- Tags List -->
            <ListBox x:Name="TagsList" Grid.Row="1" 
                     Background="Transparent" BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Margin" Value="8,4"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="bd" Background="{DynamicResource Surface0Brush}" 
                                            CornerRadius="6" Padding="12,10">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="bd" Property="Background" Value="{DynamicResource Surface1Brush}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <!-- Color Button -->
                            <Button Grid.Column="0" Style="{StaticResource ColorBtn}" 
                                    Background="{Binding Color}" Tag="{Binding Id}"
                                    Click="BtnColor_Click" ToolTip="{loc:Localize ChangeColor}" Margin="0,0,10,0"/>
                            
                            <!-- Display Mode -->
                            <StackPanel x:Name="DisplayPanel" Grid.Column="1" Orientation="Horizontal" 
                                        VerticalAlignment="Center" MouseLeftButtonDown="TagName_MouseLeftButtonDown">
                                <Border Background="{Binding Color}" CornerRadius="3" Padding="8,3" Opacity="0.9">
                                    <TextBlock Text="{Binding Name}" FontSize="12" Foreground="{DynamicResource BaseBrush}" FontWeight="Medium"/>
                                </Border>
                                <TextBlock Text="{Binding NoteCount, StringFormat=' ({0} notes)'}" 
                                           Foreground="{DynamicResource SubtextBrush}" FontSize="11" VerticalAlignment="Center" Margin="8,0,0,0"/>
                            </StackPanel>
                            
                            <!-- Edit Mode -->
                            <TextBox x:Name="EditTextBox" Grid.Column="1" 
                                     Style="{StaticResource EditTextBox}"
                                     Visibility="Collapsed" MaxLength="20"
                                     KeyDown="EditTextBox_KeyDown" LostFocus="EditTextBox_LostFocus"/>
                            
                            <!-- Actions -->
                            <StackPanel Grid.Column="2" Orientation="Horizontal">
                                <Button Content="✎" Style="{StaticResource IconBtn}" 
                                        Tag="{Binding Id}" Click="BtnEdit_Click" ToolTip="{loc:Localize Rename}"/>
                                <Button Content="✖" Style="{StaticResource IconBtn}" 
                                        Tag="{Binding Id}" Click="BtnDelete_Click" ToolTip="{loc:Localize Delete}"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            
            <!-- Color Picker Popup -->
            <Popup x:Name="ColorPickerPopup" StaysOpen="False" Placement="Mouse" AllowsTransparency="True">
                <Border Background="{DynamicResource Surface0Brush}" CornerRadius="6" 
                        BorderBrush="{DynamicResource Surface1Brush}" BorderThickness="1" Padding="8">
                    <WrapPanel x:Name="ColorPalette" Width="140">
                        <!-- Colors will be added programmatically -->
                    </WrapPanel>
                </Border>
            </Popup>
            
            <!-- Footer -->
            <Border Grid.Row="2" Background="{DynamicResource Surface0Brush}" CornerRadius="0,0,8,8">
                <Grid Margin="12,0">
                    <TextBlock x:Name="TagCount" Foreground="{DynamicResource SubtextBrush}" 
                               FontSize="12" VerticalAlignment="Center"/>
                    <Button Content="{loc:Localize NewTag}" Style="{StaticResource ActionBtn}" 
                            HorizontalAlignment="Right" Click="BtnNewTag_Click"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
