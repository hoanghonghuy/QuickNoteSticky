<Window x:Class="DevSticky.Views.ConflictResolutionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:DevSticky.Services"
        Title="{loc:Localize SyncConflictTitle}"
        Width="700" Height="500"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/SharedStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    
    <Border Background="{DynamicResource BaseBrush}" CornerRadius="8" 
            BorderBrush="{DynamicResource Surface1Brush}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="{DynamicResource Surface0Brush}" 
                    CornerRadius="8,8,0,0" Padding="16,12">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="⚠️" FontSize="20" Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <TextBlock Text="{loc:Localize SyncConflictDetected}" Foreground="{DynamicResource TextBrush}" 
                               FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center"/>
                </StackPanel>
            </Border>
            
            <!-- Description -->
            <Border Grid.Row="1" Padding="16,12">
                <StackPanel>
                    <TextBlock x:Name="NoteTitle" Foreground="{DynamicResource TextBrush}" 
                               FontSize="13" FontWeight="SemiBold" Margin="0,0,0,4"/>
                    <TextBlock Foreground="{DynamicResource Subtext0Brush}" FontSize="12" TextWrapping="Wrap">
                        This note has been modified both locally and remotely since the last sync. 
                        Choose how to resolve this conflict:
                    </TextBlock>
                </StackPanel>
            </Border>
            
            <!-- Content Comparison -->
            <Grid Grid.Row="2" Margin="16,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- Local Version -->
                <Border Grid.Column="0" Background="{DynamicResource Surface0Brush}" 
                        CornerRadius="4" Padding="8">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <TextBlock Grid.Row="0" Text="📱 Local Version" 
                                   Foreground="{DynamicResource GreenBrush}" 
                                   FontWeight="SemiBold" FontSize="12" Margin="0,0,0,4"/>
                        <TextBlock x:Name="LocalModifiedText" Grid.Row="1" 
                                   Foreground="{DynamicResource Subtext0Brush}" 
                                   FontSize="11" Margin="0,0,0,8"/>
                        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                            <ScrollViewer.Resources>
                                <Style TargetType="ScrollBar" BasedOn="{StaticResource DarkScrollBar}"/>
                            </ScrollViewer.Resources>
                            <TextBox x:Name="LocalContentText" 
                                     Background="Transparent"
                                     Foreground="{DynamicResource TextBrush}"
                                     BorderThickness="0"
                                     IsReadOnly="True"
                                     TextWrapping="Wrap"
                                     FontFamily="Consolas"
                                     FontSize="11"/>
                        </ScrollViewer>
                    </Grid>
                </Border>
                
                <!-- Remote Version -->
                <Border Grid.Column="2" Background="{DynamicResource Surface0Brush}" 
                        CornerRadius="4" Padding="8">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <TextBlock Grid.Row="0" Text="☁️ Remote Version" 
                                   Foreground="{DynamicResource BlueBrush}" 
                                   FontWeight="SemiBold" FontSize="12" Margin="0,0,0,4"/>
                        <TextBlock x:Name="RemoteModifiedText" Grid.Row="1" 
                                   Foreground="{DynamicResource Subtext0Brush}" 
                                   FontSize="11" Margin="0,0,0,8"/>
                        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                            <ScrollViewer.Resources>
                                <Style TargetType="ScrollBar" BasedOn="{StaticResource DarkScrollBar}"/>
                            </ScrollViewer.Resources>
                            <TextBox x:Name="RemoteContentText" 
                                     Background="Transparent"
                                     Foreground="{DynamicResource TextBrush}"
                                     BorderThickness="0"
                                     IsReadOnly="True"
                                     TextWrapping="Wrap"
                                     FontFamily="Consolas"
                                     FontSize="11"/>
                        </ScrollViewer>
                    </Grid>
                </Border>
            </Grid>
            
            <!-- Buttons -->
            <Border Grid.Row="3" Background="{DynamicResource Surface0Brush}" 
                    CornerRadius="0,0,8,8" Padding="16,12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <Button x:Name="CancelBtn" Grid.Column="0" Content="{loc:Localize Cancel}" 
                            Style="{StaticResource DialogBtn}" Click="CancelBtn_Click"/>
                    
                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                        <Button x:Name="KeepLocalBtn" Content="{loc:Localize KeepLocal}" 
                                Style="{StaticResource DialogBtn}" Margin="0,0,8,0"
                                Click="KeepLocalBtn_Click"/>
                        <Button x:Name="KeepRemoteBtn" Content="{loc:Localize KeepRemote}" 
                                Style="{StaticResource DialogBtn}" Margin="0,0,8,0"
                                Click="KeepRemoteBtn_Click"/>
                        <Button x:Name="MergeBtn" Content="{loc:Localize MergeBoth}" 
                                Style="{StaticResource WarningBtn}"
                                Click="MergeBtn_Click"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
