<Window x:Class="DevSticky.Views.SaveSnippetDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Save Snippet"
        Width="450" Height="Auto" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/SharedStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    
    <Border Background="{DynamicResource BaseBrush}" CornerRadius="8" 
            BorderBrush="{DynamicResource Surface1Brush}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Header -->
            <Border Grid.Row="0" Background="{DynamicResource Surface0Brush}" 
                    CornerRadius="8,8,0,0" Padding="16,12">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="💾 " FontSize="16" VerticalAlignment="Center"/>
                    <TextBlock Text="Save as Snippet" Foreground="{DynamicResource TextBrush}" 
                               FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center"/>
                </StackPanel>
            </Border>
            
            <!-- Form Content -->
            <Border Grid.Row="1" Padding="16,16,16,8">
                <StackPanel>
                    <!-- Name -->
                    <TextBlock Style="{StaticResource FieldLabel}" Text="Name *"/>
                    <TextBox x:Name="NameBox" Style="{StaticResource FieldInput}"/>
                    
                    <!-- Description -->
                    <TextBlock Style="{StaticResource FieldLabel}" Text="Description"/>
                    <TextBox x:Name="DescriptionBox" Style="{StaticResource FieldInput}" 
                             TextWrapping="Wrap" AcceptsReturn="True" Height="60"/>
                    
                    <!-- Language and Category Row -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="8"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0">
                            <TextBlock Style="{StaticResource FieldLabel}" Text="Language"/>
                            <ComboBox x:Name="LanguageCombo" Style="{StaticResource FieldCombo}"/>
                        </StackPanel>
                        
                        <StackPanel Grid.Column="2">
                            <TextBlock Style="{StaticResource FieldLabel}" Text="Category"/>
                            <ComboBox x:Name="CategoryCombo" Style="{StaticResource FieldCombo}" 
                                      IsEditable="True"/>
                        </StackPanel>
                    </Grid>
                    
                    <!-- Tags -->
                    <TextBlock Style="{StaticResource FieldLabel}" Text="Tags (comma-separated)"/>
                    <TextBox x:Name="TagsBox" Style="{StaticResource FieldInput}"/>
                    
                    <!-- Placeholder Preview -->
                    <Border x:Name="PlaceholderPreview" Background="{DynamicResource Surface0Brush}" 
                            CornerRadius="4" Padding="12" Margin="0,4,0,0" Visibility="Collapsed">
                        <StackPanel>
                            <TextBlock Text="Detected Placeholders:" 
                                       Foreground="{DynamicResource SubtextBrush}" 
                                       FontSize="11" FontWeight="SemiBold" Margin="0,0,0,8"/>
                            <ItemsControl x:Name="PlaceholderList">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="{DynamicResource Surface1Brush}" 
                                                CornerRadius="3" Padding="8,4" Margin="0,0,6,6">
                                            <TextBlock Foreground="{DynamicResource TextBrush}" FontSize="12">
                                                <Run Text="${"/><Run Text="{Binding Index}"/><Run Text=":"/><Run Text="{Binding Name}"/><Run Text="}"/>
                                            </TextBlock>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>
            
            <!-- Footer Buttons -->
            <Border Grid.Row="2" Background="{DynamicResource Surface0Brush}" 
                    CornerRadius="0,0,8,8" Padding="16,12">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Cancel" Style="{StaticResource ActionBtn}" 
                            Click="BtnCancel_Click" Margin="0,0,8,0"/>
                    <Button Content="Save" Style="{StaticResource PrimaryBtn}" 
                            Click="BtnSave_Click"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
