<Window x:Class="DevSticky.Views.TemplateSelectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:DevSticky.Services"
        Title="{loc:Localize TemplateSelectionTitle}"
        Width="700" Height="500"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/SharedStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    
    <Border Background="{DynamicResource BaseBrush}" CornerRadius="8" 
            BorderBrush="{DynamicResource Surface1Brush}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="50"/>
            </Grid.RowDefinitions>
            
            <!-- Header -->
            <Border Grid.Row="0" Background="{DynamicResource Surface0Brush}" 
                    CornerRadius="8,8,0,0" MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <Grid Margin="12,0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="📝 " Foreground="{DynamicResource TextBrush}" FontSize="14"/>
                        <TextBlock Text="{loc:Localize TemplateSelectionTitle}" Foreground="{DynamicResource TextBrush}" 
                                   FontSize="14" FontWeight="SemiBold"/>
                    </StackPanel>
                    <Button Content="✕" Style="{StaticResource IconBtn}" 
                            HorizontalAlignment="Right" Click="BtnClose_Click"/>
                </Grid>
            </Border>
            
            <!-- Search and Filter Bar -->
            <Border Grid.Row="1" Padding="12,8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Search Box -->
                    <Grid Grid.Row="0" Margin="0,0,0,8">
                        <TextBox x:Name="SearchBox"
                                 Style="{StaticResource InputBox}"
                                 TextChanged="SearchBox_TextChanged"
                                 GotFocus="SearchBox_GotFocus"
                                 LostFocus="SearchBox_LostFocus"/>
                        <TextBlock x:Name="SearchPlaceholder" Text="{loc:Localize SearchTemplates}"
                                   Foreground="{DynamicResource SubtextBrush}"
                                   IsHitTestVisible="False" VerticalAlignment="Center"
                                   Margin="10,0,0,0" FontSize="13"/>
                    </Grid>
                    
                    <!-- Category Filter -->
                    <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" 
                                  VerticalScrollBarVisibility="Disabled">
                        <StackPanel x:Name="CategoryFilterPanel" Orientation="Horizontal"/>
                    </ScrollViewer>
                </Grid>
            </Border>
            
            <!-- Template Grid -->
            <Border Grid.Row="2" Padding="8,0,8,8">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl x:Name="TemplateList">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>
                </ScrollViewer>
            </Border>
            
            <!-- Footer -->
            <Border Grid.Row="3" Background="{DynamicResource Surface0Brush}" 
                    CornerRadius="0,0,8,8" Padding="12,8">
                <Grid>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                        <TextBlock x:Name="SelectedTemplateText" Text="{loc:Localize NoTemplateSelected}" 
                                   Foreground="{DynamicResource SubtextBrush}" FontSize="12"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="{loc:Localize BlankNote}" Style="{StaticResource ActionBtn}" 
                                Click="BtnBlankNote_Click" Margin="0,0,8,0"/>
                        <Button x:Name="BtnSelect" Content="{loc:Localize UseTemplate}" Style="{StaticResource PrimaryBtn}" 
                                Click="BtnSelect_Click" IsEnabled="False"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
