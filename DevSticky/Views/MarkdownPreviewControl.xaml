<UserControl x:Class="DevSticky.Views.MarkdownPreviewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:loc="clr-namespace:DevSticky.Services"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf">
    
    <Grid>
        <!-- Loading indicator shown while WebView2 initializes -->
        <TextBlock x:Name="LoadingText"
                   Text="{loc:Localize LoadingPreview}"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Foreground="{DynamicResource SubtextBrush}"
                   FontSize="12"
                   Visibility="Visible"/>
        
        <!-- WebView2 for rendering HTML -->
        <wv2:WebView2 x:Name="WebView"
                      Visibility="Collapsed"/>
    </Grid>
</UserControl>
